class Solution(object):
    def containsDuplicate(self, nums):
        new_nums = []

        for num in nums:
            if num not in new_nums:
                new_nums.append(num)
            else:
                return True
        
        return False
